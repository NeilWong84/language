version: '3.8'

services:
  language-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: language-learning-app
    ports:
      - "80:80"
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
